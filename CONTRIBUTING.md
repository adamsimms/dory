# Contributing

Small, focused pull requests are welcome. Open an issue first for anything non-trivial.

```bash
git clone https://github.com/adamsimms/dory.git
cd dory
python3 -m http.server 8080
```

Match the style of the files you edit. Never commit deploy keys or server credentials.
